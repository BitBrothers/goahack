.container.contentContainer
  .row
    .col-md-3.sidebar
      // SIDEBAR
      .userInfo
        img(src='images/Layer-3.png')
        br
        h4 {{currentUser.profile.nameFull}}
        
        p {{currentUser.occupation}}
        p {{currentUser.location}}
        p {{currentUser.email}}
        p {{currentUser.website}}
      // </div>
      // <div class="row">
      .userSkillSet
        h4  Skills 
        p(ng-repeat='skill in currentUser.skills') {{skill}}
    // SIDEBAR ENDS
    .col-md-9.mainContent
      // MAIN CONTENT
      div(bs-active-pane='tabs.activeTab', bs-tabs='')
        div(ng-repeat='tab in tabs', title='{{ tab.title }}', ng-bind='tab.content', bs-pane='')
        div(ng-include='', src='tabs[tabs.activeTab].page')

      

    // MAIN CONTENT ENDS
