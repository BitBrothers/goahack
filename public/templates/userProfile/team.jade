.row.dashContent
  .row.dashContentTeam
    a(href='/team/{{user.events[0].team.slug}}' ng-show='user.events[0].team')
      .col-md-1.teamPhoto
        img(src='images/lannister.png')
      .col-md-10
        h4 {{user.events[0].team.name}}
      .col-md-1
        h4 x
    a(href='/teams' ng-show='!user.events[0].team') You haven't joined any teams. Click here to find one now

  
  
// dashContent ENDS